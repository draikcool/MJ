<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="actionOverflowButtonStyle">@style/ToolbarOverflow</item>-->
        <item name="android:windowBackground">@color/gray_windowbackground</item>

    </style>


    <!--Dialog样式  暂时没用到-->
    <style name="Dialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及透明度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题-->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <!--<style name="ToolbarOverflow" parent="@android:style/Widget.ActionButton.Overflow">-->
        <!--<item name="android:background">@drawable/point</item>-->
    <!--</style>-->

    <style name="Widget.MJ.Button" parent="Widget.MaterialComponents.Button.Icon">
        <item name="android:textAppearance">?android:attr/textAppearanceButton</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingBottom">5dp</item>
    </style>




</resources>
